{% extends 'base.html.twig' %}

{% block title %}Hello AnnonceController!{% endblock %}

{% block body %}


<div class="items">
    <h1>Dernières annonces</h1>

    {% for item in items %}
        <div class="item">


            <h2>{{item.title}}</h2>
            <p>{{item.description}}</p>

            <a href={{path('annonce_show',{id:item.id})}} class="btn btn-primary btn-sm">Voir</a>
        </div>
    {% else %}

        <p>Il n'y a pas encore d'annonces.</p>

    {% endfor %}
    
</div>
{% endblock %}
